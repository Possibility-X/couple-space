# 项目优化清单

## 1. 安全性优化

### 1.1 输入验证和清理
- [ ] **SQL 注入防护增强**：虽然使用了参数化查询，但部分动态 WHERE 子句（如 media.js:19-23）需要更严格的验证
- [ ] **文件上传安全**：
  - 添加文件内容类型验证（不仅检查 MIME，还要验证文件头）
  - 限制文件名长度和字符（防止路径遍历）
  - 添加病毒扫描（可选，针对生产环境）
- [ ] **XSS 防护**：前端需要对用户输入的文本（标题、描述、爱情故事等）进行转义
- [ ] **CSRF 防护**：添加 CSRF token 机制

### 1.2 认证和授权
- [ ] **密码强度要求**：Setup 时强制要求密码复杂度（最少 8 位，包含大小写字母和数字）
- [x] **登录失败限制**：添加登录失败次数限制，防止暴力破解（已实现：5次失败锁定15分钟）
- [x] **手机号双因素认证（MFA）**：登录需手机号+密码验证后再输入短信验证码（6位，5分钟有效，60秒冷却），账号限制为手机号格式（已实现：阿里云短信）
- [ ] **JWT 刷新机制**：当前 JWT 7 天过期，添加刷新 token 机制
- [ ] **会话管理**：添加主动登出功能，清除 localStorage 中的 token
- [x] **资源所有权校验**：媒体删除端点增加上传者校验，防止越权删除（已实现：403）；Gallery Lightbox 仅对上传者本人显示删除按钮（已实现）；时光轴编辑/删除无所有者限制（设计决策：双方共享时间轴）
- [x] **资源存在性校验**：相册和时间线的 PUT/DELETE 端点增加 404 检查（已实现）

### 1.3 敏感信息保护
- [x] **错误信息脱敏**：生产环境不应返回详细的数据库错误信息（已实现：返回通用错误）
- [ ] **日志脱敏**：确保日志中不包含密码、token 等敏感信息
- [x] **JWT_SECRET 强制配置**：生产环境启动时检查 JWT_SECRET 是否设置（已实现）

### 1.4 Web 安全响应头
- [x] **Nginx 安全头**：添加 X-Frame-Options、X-XSS-Protection、Referrer-Policy、Permissions-Policy（已实现）
- [x] **HSTS 配置**：HTTPS 块中标注 Strict-Transport-Security 位置（已文档化）

## 2. 性能优化

### 2.1 数据库优化
- [x] **添加索引**：（已实现）
  - `media.album_id` - 相册筛选查询
  - `media.uploaded_by` - 用户上传记录查询
  - `media.taken_at` - 按拍摄时间排序
  - `timeline_events.event_date` - 时间线排序
- [ ] **查询优化**：albums.js:9-17 的 GROUP BY 查询可能在大量数据时变慢，考虑缓存或优化
- [x] **分页参数校验**：limit 上限 100，page 最小 1，防止 DoS 攻击（已实现）
- [ ] **分页优化**：分页查询在海量数据时可改用游标分页代替 OFFSET

### 2.2 文件处理优化
- [ ] **缩略图生成异步化**：当前上传时同步生成缩略图，大文件会阻塞响应，应改为后台任务
- [ ] **图片压缩**：上传时自动压缩原图（可选，保留原图但生成压缩版用于展示）
- [ ] **视频缩略图**：当前视频没有缩略图，可以使用 ffmpeg 生成视频首帧作为缩略图
- [ ] **渐进式加载**：前端实现图片懒加载和渐进式 JPEG
- [x] **文件删除异步化**：媒体删除改用 fs.promises.unlink，失败时记录日志（已实现）

### 2.3 前端性能
- [ ] **代码分割**：使用 Vue Router 的懒加载（已实现）+ 组件级别的懒加载
- [ ] **图片优化**：使用 WebP 格式（带 fallback）
- [ ] **缓存策略**：添加 Service Worker 实现离线缓存
- [ ] **虚拟滚动**：Gallery 和 Timeline 在大量数据时使用虚拟滚动

## 3. 用户体验优化

### 3.1 功能增强
- [ ] **批量操作**：
  - 批量删除照片
  - 批量移动照片到相册
  - 批量下载
- [ ] **搜索功能**：按标题、描述、日期搜索照片和时间线事件
- [ ] **标签系统**：为照片添加标签，支持按标签筛选
- [ ] **评论功能**：双方可以对照片和时间线事件添加评论
- [ ] **通知系统**：对方上传新照片或添加时间线事件时通知

### 3.2 交互优化
- [ ] **上传进度优化**：
  - 显示每个文件的上传进度百分比
  - 支持暂停/恢复上传
  - 上传失败时支持重试
- [ ] **拖拽排序**：相册和时间线事件支持拖拽排序
- [ ] **快捷键支持**：Lightbox 中添加键盘快捷键（左右箭头切换，ESC 关闭）
- [ ] **移动端优化**：
  - 手势支持（双指缩放、滑动切换）
  - 底部导航栏优化
  - 适配刘海屏和安全区域

### 3.3 视觉优化
- [x] **加载状态**：Home 页面添加 loading 状态，Gallery/Timeline 已有 loading（已实现）
- [ ] **空状态设计**：相册、时间线为空时显示友好的空状态提示
- [ ] **错误提示优化**：使用 toast 或 notification 组件统一错误提示（当前 Timeline 用 alert）
- [ ] **动画优化**：页面切换、列表加载添加流畅的过渡动画

## 4. 功能完善

### 4.1 核心功能
- [ ] **视频播放优化**：
  - 添加视频播放器控件
  - 支持倍速播放
  - 支持画中画模式
- [ ] **EXIF 信息提取**：
  - 自动提取照片的拍摄时间、地点、设备信息
  - 显示照片的 EXIF 信息
- [ ] **地图功能**：如果照片包含 GPS 信息，在地图上显示拍摄地点
- [ ] **回忆功能**：
  - "一年前的今天"提醒
  - 随机回忆（随机展示过去的照片）

### 4.2 数据管理
- [ ] **导出功能**：
  - 导出所有照片和数据
  - 导出时间线为 PDF
- [ ] **备份提醒**：定期提醒用户备份数据
- [ ] **存储空间统计**：显示已使用的存储空间和剩余空间

### 4.3 个性化
- [ ] **主题切换**：支持多种主题颜色
- [ ] **头像上传**：用户可以上传和更换头像
- [ ] **个性化设置**：
  - 自定义纪念日提醒
  - 自定义首页显示内容
  - 自定义时间线样式

## 5. 代码质量

### 5.1 错误处理
- [x] **前端异步操作错误处理**：Gallery、Timeline、Home 所有异步操作添加 try-catch（已实现）
- [x] **后端全局错误处理中间件**：生产环境不暴露错误详情（已实现）
- [ ] **前端全局错误拦截器**：统一处理 401/403/500 等状态码
- [ ] **错误日志**：记录所有错误到日志文件
- [ ] **优雅降级**：缩略图生成失败时使用原图，不影响上传流程

### 5.2 代码规范
- [ ] **ESLint 配置**：添加 ESLint 和 Prettier 配置
- [ ] **TypeScript 迁移**：考虑将前端迁移到 TypeScript（可选）
- [ ] **API 文档**：使用 Swagger/OpenAPI 生成 API 文档
- [ ] **单元测试**：为关键业务逻辑添加单元测试

### 5.3 重构建议
- [ ] **前端状态管理优化**：统一使用 Pinia，避免组件间直接传递复杂状态
- [ ] **API 请求封装**：封装统一的 API 请求方法，处理 token、错误等
- [ ] **组件复用**：提取通用组件（如 Modal、Toast、Loading）

## 6. 运维和监控

### 6.1 日志和监控
- [ ] **访问日志**：Nginx 访问日志持久化
- [ ] **应用日志**：后端添加结构化日志（使用 winston 或 pino）
- [ ] **性能监控**：
  - 添加 API 响应时间监控
  - 数据库查询性能监控
- [ ] **错误追踪**：集成 Sentry 或类似服务（可选）

### 6.2 部署优化
- [x] **健康检查**：docker-compose 后端服务添加 healthcheck（已实现）
- [ ] **优雅关闭**：处理 SIGTERM 信号，确保请求处理完成后再关闭
- [x] **环境变量验证**：启动时验证 JWT_SECRET 等必需变量是否存在（已实现）
- [ ] **Docker 镜像优化**：使用多阶段构建减小镜像体积
- [x] **.dockerignore**：排除 .env / .git / node_modules 等不必要文件（已实现）
- [x] **CORS 生产环境配置**：通过 CORS_ORIGIN 环境变量指定允许的前端域名（已实现）

### 6.3 备份和恢复
- [ ] **自动备份脚本**：定时备份数据库和上传文件
- [ ] **备份验证**：定期验证备份文件的完整性
- [ ] **恢复测试**：定期测试备份恢复流程
- [ ] **异地备份**：将备份文件同步到云存储（可选）

### 6.4 SSL 和证书管理
- [ ] **证书自动续期**：配置 Certbot 自动续期（cron job）
- [ ] **证书过期监控**：添加证书过期提醒
- [ ] **HTTPS 强制**：确保所有 HTTP 请求重定向到 HTTPS

## 7. 文档完善

- [ ] **API 文档**：详细的 API 接口文档
- [ ] **开发指南**：详细的本地开发环境搭建指南
- [ ] **贡献指南**：如果开源，添加 CONTRIBUTING.md
- [ ] **变更日志**：维护 CHANGELOG.md 记录版本变更
- [ ] **故障排查指南**：常见问题和解决方案

## 优先级建议

### P0（已完成 ✅）
1. ~~资源所有权校验（媒体越权删除）~~
2. ~~Nginx 安全响应头~~
3. ~~错误信息脱敏（生产环境）~~
4. ~~前端路由守卫 Setup 绕过漏洞~~
5. ~~JWT_SECRET 生产环境强制校验~~

### P1（已完成 ✅）
1. ~~分页参数上限校验（DoS 防护）~~
2. ~~数据库关键索引（4个）~~
3. ~~CORS 生产环境配置~~
4. ~~前端异步操作 try-catch~~
5. ~~文件删除异步化~~
6. ~~手机号双因素认证（MFA）—— 手机号+密码+短信 OTP，阿里云短信~~

### P2（待处理）
1. 输入验证和字段长度校验
2. 文件上传安全增强（文件头验证）
3. 前端全局 axios 拦截器封装
4. 视频缩略图
5. 加载状态和 toast 错误提示统一

### P3（低优先级 - 功能增强）
1. 搜索功能
2. 批量操作
3. 标签系统
4. 评论功能
5. 主题切换
6. 地图功能
7. 导出功能
8. SSL 证书自动续期
